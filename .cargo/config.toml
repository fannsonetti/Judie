# Used by CI when cross-compiling the Linux .deb to 32-bit ARM.
# Native armv7 builds (on the Pi) do not pass --target, so this is unused there.
[target.armv7-unknown-linux-gnueabihf]
linker = "arm-linux-gnueabihf-gcc"
